Flask
flask-cors
gunicorn
firebase-admin
requests

numpy>=1.22,<1.24

# ⚠️ CPU-only wheels (เล็กกว่า/เร็วกว่าตอน build มาก เพราะไม่มี GPU ให้ใช้อยู่แล้วใน service นี้)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

ultralytics==8.3.0
onnx>=1.14.0
onnxscript>=0.1.0

# หมายเหตุ: ถ้าต้องการ export format "tflite" ด้วย ต้องเพิ่ม tensorflow กลับเข้ามา
# (จะทำให้ image ใหญ่ขึ้นและ build ช้าลง) — ถ้าใช้แค่ onnx ไม่ต้องเพิ่ม


 